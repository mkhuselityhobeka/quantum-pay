spring:
  datasource:
      driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
      url: jdbc:tc:mysql://localhost:3306/quantum-pay-test
      username: root
      password: admin

  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
      generate-ddl: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
